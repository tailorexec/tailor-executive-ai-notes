node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
.DS_Store
.vscode/*
!.vscode/extensions.json
npm-debug.log*
yarn-debug.log*
yarn-error.log*
supabase/.temp

.vercel
.env*

# APK baixado do workflow (nao versionar binario)
apk-out/

# Keystores / credenciais de assinatura (NUNCA versionar)
*.jks
*.keystore
keystore.properties

# Instalador Windows gerado pelo electron-builder (nao versionar binario)
release/
# Icone .ico gerado a partir de assets/icon.png (`npm run electron:icon`) -- nao versionar.
# build/* (nao build/) pra conseguir re-incluir um arquivo especifico abaixo.
build/*
# ...menos o script NSIS do instalador, que E versionado (fecha o app da bandeja no update).
!build/installer.nsh

# Certificado de assinatura de codigo Windows (NUNCA versionar)
*.pfx
*.p12
